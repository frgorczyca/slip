import { Title as MantineTitle } from '@mantine/core';

import styles from './styles.module.css';

export const Title = () => {
  return <MantineTitle className={styles.title}>Slip</MantineTitle>;
};
